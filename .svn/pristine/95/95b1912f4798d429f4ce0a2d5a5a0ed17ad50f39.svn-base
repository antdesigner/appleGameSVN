
using System.Collections.Generic;
using System.Linq;
using Microsoft.AspNetCore.Mvc;
using AntDesigner.AppleGame.EF;
using Microsoft.AspNetCore.Http;
using AntDesigner.AppleGame.interFace;

// For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860

namespace AntDesigner.AppleGame.Controllers
{
    public class AccountController : myController
    {
        //public AccountController(DataContext db, IHttpContextAccessor httpContextAccessor) : base(db, httpContextAccessor)
        //{

        //}
        public AccountController(IStorehouse IStorehouse,IHttpContextAccessor httpContextAccessor) : base(IStorehouse,httpContextAccessor)
        {

        }

        public IActionResult accountDetail()
        {

            List<AccountDetail> accountDetails = player.getAccountDetails().OrderByDescending(a=>a.createTime).ToList();
            ViewBag.account = player.account;
            return View("accountDetail",accountDetails);
        }

       
    }
}
